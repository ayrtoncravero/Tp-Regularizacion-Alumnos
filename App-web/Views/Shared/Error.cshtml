@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">Error.</h1>
<h2 class="text-danger">Se produjo un error al procesar su solicitud.</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>ID de solicitud:</strong> <code>@Model.RequestId</code>
    </p>
}

<h3>Modo desarrollador</h3>
<p>
    Cambiando a <strong>Desarrollador</strong> El entorno mostrará información más detallada sobre el error que se produjo.
</p>
<p>
    <strong>El entorno de desarrollo no debe habilitarse para las aplicaciones implementadas.</strong>
    Puede resultar en mostrar información confidencial de excepciones a los usuarios finales.
    Para la depuración local, habilite el <strong>Desarrollador</strong> entorno estableciendo el <strong>ASPNETCORE_ENVIRONMENT</strong> environment variable to <strong>Development</strong>
    and restarting the app.
</p>
